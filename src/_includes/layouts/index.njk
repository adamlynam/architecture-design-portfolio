<!doctype html>
<html class="h-100" lang="{{ metadata.language }}">
  <head>
    {% include 'snippets/head.njk' %}
  </head>
  <body>
    {% include 'snippets/navbar.njk' %} <img
      class="w-100 position-absolute"
      style="z-index:-10; height:25em; object-fit:cover;overflow: hidden;"
      src="./img/main_bg.png"/>
    <main class="container py-vh-0">
      <div class="row d-flex justify-content-center">
        <div class="py-vh-4 col-12 col-xl-8 my-4 text-center">
          <h1 class=" display-4">{{ title }}</h1>
          <p class="fs-6">{{ subtitle }}</p>
          <p class="fs-6">
            <a target="_blank" href={{ portfolio_document_url }}>Check out my portfolio</a>
            for a glance at my student work, or click the images below to see more detailed drawing sets from student projects.
          </p>
          <a target="_blank" class="link-fancy pt-4 position-relative" href={{ linkedin_url }}>
            <p class="fs-6 mb-0">
              {{ contact_text }}</p>
          </a>
        </div>
      </div>
    </main>
    <div class="">
      {% include 'snippets/projectloop.njk' %}
    </div>
    <footer class="container">
      {% include 'snippets/footer.njk' %}</footer>
  </body>
</html>